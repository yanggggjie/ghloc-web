import { clsx } from 'clsx'
import _ from 'lodash-es'
interface Props {}

function Component({}: Props) {
  return (
    <div className={clsx('')}>Count lines of code in a GitHub repository</div>
  )
}

export default Component
